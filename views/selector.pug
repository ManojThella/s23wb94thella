extends layout

block content
  h1= title
  - var image_names=["D1.webp","D2.jpg","D3.jpg","D4.jpg","D5.webp"]
  - for(let i=0; i<3; i++){
      - let rand = Math.floor(Math.random()*5)
      img(src=`/images/${image_names[rand]}`, height="300px", width="300px", alt=`${image_names[rand]}`)
  - }